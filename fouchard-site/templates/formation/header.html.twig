{% block header %}
    <div class="{{ background_image_css }}">
        <div class="container">
            <div class="presentation_title">
                {{ title }}
            </div>
            <div class="presentation_description">
                {{ description }}
            </div>
            <div class="row">
                <a href="{{ href }}" style="padding-top: 12px" class="col s7 m3 offset-m8 hoverable button_white">
                    {{ button_title }}
                </a>
            </div>
        </div>
    </div>
{% endblock %}
